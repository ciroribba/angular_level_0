const name = 'John';
let hpPoints: number | 'FULL' = 20;
hpPoints = 'FULL';

let isAlive: boolean = true;

let skills: string[] = ['bash', 'counter', 'healing'];
let hpPints: number[] = [1, 2, 3];

console.log({name, hpPoints, isAlive, skills, hpPints});

export {};